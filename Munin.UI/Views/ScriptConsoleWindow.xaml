<Window x:Class="Munin.UI.Views.ScriptConsoleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:Munin.UI.Localization"
        Title="{loc:Localize Key=MainWindow_ScriptConsole}" 
        Height="450" Width="650"
        MinHeight="300" MinWidth="400"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        Background="Transparent"
        WindowStartupLocation="CenterOwner">
    
    <Border Background="{StaticResource BackgroundDarkBrush}"
            CornerRadius="8"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Custom Title Bar -->
            <Border Grid.Row="0" 
                    Background="{StaticResource BackgroundMediumBrush}"
                    CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="36">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Title -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="⚡" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Script Console" 
                                   FontSize="13" 
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- Window Controls -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Click="MinimizeButton_Click"
                                Style="{StaticResource WindowControlButtonStyle}">
                            <Path Data="M0,5 H10" 
                                  Stroke="{StaticResource TextSecondaryBrush}" 
                                  StrokeThickness="1"
                                  Width="10" Height="10"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"/>
                        </Button>
                        <Button Click="CloseButton_Click"
                                Style="{StaticResource WindowCloseButtonStyle}">
                            <Path Data="M0,0 L10,10 M10,0 L0,10" 
                                  Stroke="{StaticResource TextSecondaryBrush}" 
                                  StrokeThickness="1"
                                  Width="10" Height="10"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Content -->
            <Grid Grid.Row="1" Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Output area -->
                <Border Grid.Row="0" 
                        Background="{StaticResource BackgroundLightBrush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="4">
                    <TextBox x:Name="OutputTextBox"
                             FontFamily="Consolas"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             Background="Transparent"
                             BorderThickness="0"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             Padding="8"
                             AcceptsReturn="True"/>
                </Border>
                
                <!-- Input area -->
                <Grid Grid.Row="1" Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox x:Name="InputTextBox"
                             Grid.Column="0"
                             FontFamily="Consolas"
                             Padding="8"
                             KeyDown="InputTextBox_KeyDown"/>
                    
                    <Button Grid.Column="1" 
                            Content="{loc:Localize Key=ScriptConsole_Run}" 
                            Padding="15,8"
                            Margin="8,0,0,0"
                            Click="RunButton_Click"/>
                </Grid>
                
                <!-- Script status -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock Text="{loc:Localize Key=ScriptConsole_LoadedScripts}" 
                               Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBlock x:Name="ScriptCountText" 
                               Text="0"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <Button Content="Reload All" 
                            Margin="20,0,0,0"
                            Padding="10,4"
                            Click="ReloadAllButton_Click"/>
                    <Button Content="{loc:Localize Key=ScriptConsole_Clear}" 
                            Margin="8,0,0,0"
                            Padding="10,4"
                            Click="ClearButton_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
