<Window x:Class="Munin.UI.Views.SecurityLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:Munin.UI.Localization"
        Title="{loc:Localize Key=SecurityLog_Title}"
        Height="500"
        Width="700"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Foreground="{StaticResource TextPrimaryBrush}"
        FontFamily="{StaticResource UIFont}"
        FontSize="13">

    <Border Background="{StaticResource BackgroundDarkBrush}"
            CornerRadius="8"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Border Grid.Row="0" 
                    Background="{StaticResource BackgroundMediumBrush}"
                    CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="38">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="🛡️" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="{loc:Localize Key=SecurityLog_Title}" 
                                   FontSize="13" 
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <Button Grid.Column="1" 
                            Click="CloseButton_Click"
                            Style="{StaticResource WindowCloseButtonStyle}"
                            ToolTip="{loc:Localize Key=MainWindow_Close}">
                        <Path Data="M0,0 L10,10 M10,0 L0,10" 
                              Stroke="{StaticResource TextSecondaryBrush}" 
                              StrokeThickness="1"
                              Width="10" Height="10"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Center"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Header -->
            <Border Grid.Row="1" 
                    Background="{StaticResource BackgroundMediumBrush}"
                    BorderBrush="{StaticResource BorderSubtleBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="20,16">
                <StackPanel Orientation="Horizontal">
                    <Border Width="36" Height="36" 
                            CornerRadius="8"
                            Background="{StaticResource AccentSubtleBrush}"
                            Margin="0,0,14,0">
                        <TextBlock Text="🛡️" FontSize="18" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{loc:Localize Key=SecurityLog_Title}" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"/>
                        <TextBlock Text="{loc:Localize Key=SecurityLog_Description}" 
                                   Foreground="{StaticResource TextMutedBrush}"
                                   FontSize="12"
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Event List -->
            <Border Grid.Row="2" 
                    Background="{StaticResource BackgroundMediumBrush}"
                    BorderBrush="{StaticResource BorderSubtleBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Margin="20,16,20,0">
            <DataGrid x:Name="EventsDataGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      Background="Transparent"
                      Foreground="{StaticResource TextPrimaryBrush}"
                      BorderThickness="0"
                      FontFamily="{StaticResource MonospaceFont}"
                      FontSize="12"
                      RowBackground="{StaticResource BackgroundMediumBrush}"
                      AlternatingRowBackground="{StaticResource BackgroundLightBrush}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{loc:Localize Key=SecurityLog_Time}" 
                                        Binding="{Binding Timestamp, StringFormat='{}{0:dd.MM.yyyy HH:mm:ss}'}" 
                                        Width="145"/>
                    <DataGridTextColumn Header="{loc:Localize Key=SecurityLog_Status}" 
                                        Binding="{Binding StatusIcon}" 
                                        Width="50"/>
                    <DataGridTextColumn Header="{loc:Localize Key=SecurityLog_Event}" 
                                        Binding="{Binding Description}" 
                                        Width="*"/>
                    <DataGridTextColumn Header="{loc:Localize Key=SecurityLog_Machine}" 
                                        Binding="{Binding MachineName}" 
                                        Width="100"/>
                    <DataGridTextColumn Header="{loc:Localize Key=SecurityLog_User}" 
                                        Binding="{Binding UserName}" 
                                        Width="100"/>
                </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Buttons -->
            <Border Grid.Row="3"
                    BorderBrush="{StaticResource BorderSubtleBrush}"
                    BorderThickness="0,1,0,0"
                    Margin="0,16,0,0"
                    Padding="20,16">
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Right">
                    <Button Content="{loc:Localize Key=SecurityLog_Refresh}" 
                            Click="RefreshButton_Click"
                            Padding="20,10"
                            Margin="0,0,12,0"/>
                    <Button Content="{loc:Localize Key=Close}" 
                            Click="CloseButton_Click"
                            Style="{StaticResource AccentButtonStyle}"
                            Padding="20,10"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
