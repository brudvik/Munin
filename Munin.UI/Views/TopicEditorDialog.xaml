<Window x:Class="Munin.UI.Views.TopicEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:Munin.UI.Localization"
        Title="Edit Topic"
        Height="280" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Icon="/Resources/munin-icon.ico">
    
    <Border Background="{StaticResource BackgroundDarkBrush}" 
            BorderBrush="{StaticResource BorderBrush}" 
            BorderThickness="1"
            CornerRadius="8">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title Bar -->
        <Border Grid.Row="0" 
                Background="{StaticResource BackgroundMediumBrush}" 
                CornerRadius="8,8,0,0"
                MouseDown="TitleBar_MouseDown">
            <Grid Height="40">
                <TextBlock Text="Edit Channel Topic"
                           FontSize="14" 
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           VerticalAlignment="Center"
                           Margin="16,0,0,0"/>
                <Button Content="✕" 
                        HorizontalAlignment="Right"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="{StaticResource TextSecondaryBrush}"
                        Cursor="Hand"
                        Click="CloseButton_Click"
                        Padding="12,8"
                        FontSize="14"/>
            </Grid>
        </Border>
        
        <!-- Content -->
        <StackPanel Grid.Row="1" Margin="20">
            <TextBlock Text="Topic:"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="13"
                       Margin="0,0,0,8"/>
            
            <TextBox x:Name="TopicTextBox"
                     AcceptsReturn="False"
                     TextWrapping="NoWrap"
                     Height="32"
                     Background="{StaticResource BackgroundElevatedBrush}"
                     Foreground="{StaticResource TextPrimaryBrush}"
                     BorderBrush="{StaticResource BorderSubtleBrush}"
                     Padding="8,6"
                     VerticalContentAlignment="Center"/>
            
            <!-- Encryption option (only visible if channel has encryption key) -->
            <StackPanel x:Name="EncryptionPanel" 
                        Visibility="Collapsed"
                        Margin="0,16,0,0">
                <CheckBox x:Name="EncryptCheckBox"
                          Foreground="{StaticResource TextPrimaryBrush}">
                    <TextBlock Text="Encrypt topic with FiSH"/>
                </CheckBox>
                <TextBlock Text="Note: Encrypted topics can only be read by users with the same key."
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="11"
                           Margin="20,4,0,0"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </StackPanel>
        
        <!-- Buttons -->
        <Border Grid.Row="2" 
                Background="{StaticResource BackgroundMediumBrush}"
                CornerRadius="0,0,8,8"
                Padding="20,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel"
                        Click="CancelButton_Click"
                        Background="Transparent"
                        BorderBrush="{StaticResource BorderSubtleBrush}"
                        Foreground="{StaticResource TextSecondaryBrush}"
                        Padding="16,8"
                        Margin="0,0,12,0"
                        Cursor="Hand"/>
                <Button Content="Set Topic"
                        Click="SetTopicButton_Click"
                        Background="{StaticResource AccentBrush}"
                        BorderThickness="0"
                        Foreground="White"
                        Padding="16,8"
                        Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
    </Border>
</Window>
